import { NextPage } from "next";
import { Button } from "~/components";

const Home: NextPage = () => {
  return (
    <main>
      {/* sections goes here */}
      <h1 style={{ color: "white" }}>the index page</h1>
    </main>
  );
};

export default Home;
